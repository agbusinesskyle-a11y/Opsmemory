# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.gpg
*.asc
# Public GPG keys are safe to commit (they're public by design).
!infra/keys/*.asc

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/

# Node / web
node_modules/
*.log
.npm/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Backups / dumps (local-only artifacts)
*.dump
*.dump.gpg
*.sql.gz
backups/
restore-tmp/

# Build outputs
dist/
out/

# Coverage
.coverage
htmlcov/
.pytest_cache/

# Operator-only references (real tunnel UUIDs, team domains, owner emails, etc.)
# Use `.local/*` (not `.local/`) so the `!` negation can re-include the example.
.local/*
!.local/owners.example.json
docs/secrets-references.md
seeds.users.json
seeds.users.local.json
